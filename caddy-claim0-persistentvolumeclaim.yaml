apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: caddy-claim0
  name: caddy-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
