apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nginx-password
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend-access
    kind: ClusterSecretStore
  target:
    name: nginx-password
    creationPolicy: Owner
  data:
    - secretKey: acceder
      remoteRef:
        key: nginx/data/auth
        property: data.acceder
    - secretKey: admin
      remoteRef: 
        key: nginx/data/auth
        property: data.admin
