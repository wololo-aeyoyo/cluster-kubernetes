apiVersion: v1
kind: Service
metadata:
  name: resume-web-service
  namespace: personal-web-site
  labels:
    app: resume-web
spec:
  type: ClusterIP
  selector:
    app: resume-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
